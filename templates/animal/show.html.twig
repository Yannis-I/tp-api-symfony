{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ animal.nom }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ animal.taille }}</td>
            </tr>
            <tr>
                <th>DureeDeVie</th>
                <td>{{ animal.dureeDeVie }}</td>
            </tr>
            <tr>
                <th>ArtMartial</th>
                <td>{{ animal.artMartial }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ animal.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animal_index') }}">back to list</a>

    <a href="{{ path('app_animal_edit', {'id': animal.id}) }}">edit</a>

    {{ include('animal/_delete_form.html.twig') }}
{% endblock %}
